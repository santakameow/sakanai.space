Portfolio site scaffold

Files created:
- index.html (personalized for Sakanai)
- styles.css

Preview locally:

```bash
# from the project folder
python -m http.server 8000
# then open http://localhost:8000 in your browser
```

Notes:
- Replace or add projects in `index.html` under the "Projects" section.
- Replace `resume.pdf` if you add one to the folder.
- Add images to an `assets/` folder and reference them from the HTML.
